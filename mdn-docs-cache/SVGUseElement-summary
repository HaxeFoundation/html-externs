The <strong><code>SVGUseElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/use" title="The &lt;use&gt; element takes nodes from within the SVG document, and duplicates them somewhere else."><code>&lt;use&gt;</code></a> element.