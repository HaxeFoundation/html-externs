The <code>TouchList</code> interface represents a list of contact points with a touch surface; for example, if the user has three fingers on the touch surface (such as a screen or trackpad), the corresponding <code>TouchList</code> object would have one <a href="/en-US/docs/Web/API/Touch" title="The Touch interface represents a single contact point on a touch-sensitive device. The contact point is commonly a finger or stylus and the device may be a touchscreen or trackpad."><code>Touch</code></a> object for each finger, for a total of three entries.