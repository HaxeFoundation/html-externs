The <strong><code>SVGDefsElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/defs" title="SVG allows graphical objects to be defined for later reuse. It is recommended that, wherever possible, referenced elements be defined inside of a &lt;defs&gt; element. Objects created inside a &lt;defs&gt; element are not rendered immediately; instead, think of them as templates or macros created for future use."><code>&lt;defs&gt;</code></a> element.