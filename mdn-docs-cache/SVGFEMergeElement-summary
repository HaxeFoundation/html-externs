The <strong><code>SVGFEMergeElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/feMerge" title="The &lt;feMerge&gt; SVG element allows filter effects to be applied concurrently instead of sequentially. This is achieved by other filters storing their output via the result attribute and then accessing it in a &lt;feMergeNode&gt; child."><code>&lt;feMerge&gt;</code></a> element.