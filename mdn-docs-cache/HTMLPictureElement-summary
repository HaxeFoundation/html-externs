The <code><strong>HTMLPictureElement</strong></code> interface represents a <a href="/en-US/docs/Web/HTML/Element/picture" title="The HTML &lt;picture&gt; element serves as a container for zero or more &lt;source&gt; elements and one &lt;img&gt; element to provide versions of an image for different display device scenarios."><code>&lt;picture&gt;</code></a> HTML element. It doesn't implement specific properties or methods.