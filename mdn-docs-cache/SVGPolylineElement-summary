The <strong><code>SVGPolylineElement</code></strong> interface provides access to the properties of <a href="/en-US/docs/Web/SVG/Element/polyline" title="The &lt;polyline&gt; SVG element is an SVG basic shape that creates straight lines connecting several points. Typically a polyline is used to create open shapes as the last point doesn't have to be connected to the first point. For closed shapes see the &lt;polygon&gt; element."><code>&lt;polyline&gt;</code></a> elements, as well as methods to manipulate them.