The <strong><code>BlobEvent</code></strong> interface represents events associated with a <a href="/en-US/docs/Web/API/Blob" title="A Blob object represents a file-like object of immutable, raw data. Blobs represent data that isn't necessarily in a JavaScript-native format. The File interface is based on Blob, inheriting blob functionality and expanding it to support files on the user's system."><code>Blob</code></a>. These blobs are typically, but not necessarily,  associated with media content.