The <strong><code>SVGFEOffsetElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/feOffset" title="The &lt;feOffset&gt; SVG filter primitive allows to offset the input image. The input image as a whole is offset by the values specified in the dx and dy attributes."><code>&lt;feOffset&gt;</code></a> element.