The <strong><code>SVGScriptElement</code></strong> interface corresponds to the SVG <a href="/en-US/docs/Web/SVG/Element/script" title="A SVG script element is equivalent to the script element in HTML and thus is the place for scripts (e.g., ECMAScript)."><code>&lt;script&gt;</code></a> element.