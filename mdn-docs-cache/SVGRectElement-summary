The <code>SVGRectElement</code> interface provides access to the properties of <a href="/en-US/docs/Web/SVG/Element/rect" title="The &lt;rect&gt; element is a basic SVG shape that creates rectangles, defined by their corner's position, their width, and their height. The rectangles may have their corners rounded."><code>&lt;rect&gt;</code></a> elements, as well as methods to manipulate them.