<strong><code>CSSStyleRule</code></strong> represents a single CSS style rule. It implements the <a href="/en-US/docs/Web/API/CSSRule" title="The CSSRule interface represents a single CSS rule. There are several types of rules, listed in the Type constants section below."><code>CSSRule</code></a> interface with a type value of <code>1</code> (<code>CSSRule.STYLE_RULE</code>).