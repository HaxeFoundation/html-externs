The <strong><code>SVGFETurbulenceElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/feTurbulence" title="The &lt;feTurbulence&gt; SVG filter primitive creates an image using the Perlin turbulence function. It allows the synthesis of artificial textures like clouds or marble. The resulting image will fill the entire filter primitive subregion."><code>&lt;feTurbulence&gt;</code></a> element.