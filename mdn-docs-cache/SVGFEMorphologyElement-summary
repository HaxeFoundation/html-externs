The <strong><code>SVGFEMorphologyElement</code></strong> interface corresponds to the <a href="/en-US/docs/Web/SVG/Element/feMorphology" title="The &lt;feMorphology&gt; SVG filter primitive is used to erode or dilate the input image. It's usefulness lies especially in fattening or thinning effects."><code>&lt;feMorphology&gt;</code></a> element.