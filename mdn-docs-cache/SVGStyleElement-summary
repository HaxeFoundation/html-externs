The <strong><code>SVGStyleElement</code></strong> interface corresponds to the SVG <a href="/en-US/docs/Web/SVG/Element/style" title="The &lt;style&gt; SVG element allows style sheets to be embedded directly within SVG content. SVG's style element has the same attributes as the corresponding element in HTML (see HTML's &lt;style&gt; element)."><code>&lt;style&gt;</code></a> element.