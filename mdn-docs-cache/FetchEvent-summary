This is the event type for <code>fetch</code> events dispatched on the <a href="/en-US/docs/Web/API/ServiceWorkerGlobalScope" title="The ServiceWorkerGlobalScope interface of the ServiceWorker API represents the global execution context of a service worker.">service worker global scope</a>. It contains information about the fetch, including the request and how the receiver will treat the response. It provides the <a href="/en-US/docs/Web/API/FetchEvent/respondWith" title="The respondWith() method of FetchEvent prevents the browser's default fetch handling, and allows you to provide a promise for a Response yourself."><code>event.respondWith()</code></a> method, which allows us to provide a response to this fetch.