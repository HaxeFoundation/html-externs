<strong><code>CSSPageRule</code></strong> is an interface representing a single CSS <a href="/en-US/docs/Web/CSS/@page" title="The @page CSS at-rule is used to modify some CSS properties when printing a document. You can't change all CSS properties with @page. You can only change the margins, orphans, widows, and page breaks of the document. Attempts to change any other CSS properties will be ignored."><code>@page</code></a> rule. It implements the <a href="/en-US/docs/Web/API/CSSRule" title="The CSSRule interface represents a single CSS rule. There are several types of rules, listed in the Type constants section below."><code>CSSRule</code></a> interface with a type value of <code>6</code> (<code>CSSRule.PAGE_RULE</code>).